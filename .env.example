# 本地开发环境变量（勿提交真实值）
DATABASE_URL="postgresql://oj:oj_dev_pw_2026@localhost:5432/oj_practice"

# JWT 签名密钥：本地用开发值；生产必须换成 64 位随机串并严格校验（部署清单会写）
JWT_SECRET="dev-only-secret-change-in-production-20260904"

# 站点 URL（sitemap / canonical / 登录跳转用）
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
